/* facename fixed
 * copyright Public domain font.  Share and enjoy.
 *
 * height 13
 * width 6
 *
 * Full layout of all 256 ASCII characters in 6x13 bit format. 
 * Each caracter layout is an array of 13 shorts, with each 1 bit
 * representing a forground pixel, and each 0 bit representing a background 
 * pixel (forground and background colors being defined by the display at that
 * time).
 */
 
 


extern const  char characters[][13];
